# Backend Environment Variables
PORT=5000
MONGODB_URI=mongodb://localhost:27017/footer-sections

# Optional: For production MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/footer-sections?retryWrites=true&w=majority

# Optional: JWT Secret for authentication (if needed later)
# JWT_SECRET=your-super-secret-jwt-key

# Optional: CORS Origin (for production)
# CORS_ORIGIN=http://localhost:3000 